package com.forumx.notification.entity;

/** Domain object types that a notification may reference. */
public enum ReferenceType {
    QUESTION,
    ANSWER,
    COMMENT,
    USER,
    SYSTEM,
    TICKET,
    TICKET_MESSAGE,
    REPORT
}
